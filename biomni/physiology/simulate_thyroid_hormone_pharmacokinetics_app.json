{
  "name": "simulate_thyroid_hormone_pharmacokinetics",
  "description": "Simulates the transport and binding of thyroid hormones across different tissue compartments using an ODE-based pharmacokinetic model.",
  "icon": "🧬",
  "container": {
    "image": "python:3.9-slim",
    "setup_commands": [
      "git clone --depth 1 https://github.com/snap-stanford/Biomni.git /tmp/biomni && cd /tmp/biomni && pip install -e . && rm -rf .git"
    ]
  },
  "inputs": [
    {
      "name": "parameters",
      "description": "Dictionary containing model parameters: transport_rates, binding_constants, metabolism_rates, and volumes",
      "type": "object",
      "required": true
    },
    {
      "name": "initial_conditions",
      "description": "Dictionary of initial concentrations for all molecular species in each compartment",
      "type": "object",
      "required": true
    },
    {
      "name": "time_span",
      "description": "Start and end time for simulation in hours",
      "type": "array",
      "required": false,
      "default": [0, 24]
    },
    {
      "name": "time_points",
      "description": "Number of time points to output",
      "type": "integer",
      "required": false,
      "default": 100
    }
  ],
  "output": {
    "type": "string",
    "description": "Research log summarizing the simulation and results"
  }
}
