{
  "name": "analyze_protein_colocalization",
  "description": "Analyze colocalization between two fluorescently labeled proteins in microscopy images.",
  "icon": "🔬",
  "container": {
    "image": "python:3.9-slim",
    "setup_commands": [
      "git clone --depth 1 https://github.com/snap-stanford/Biomni.git /tmp/biomni && cd /tmp/biomni && pip install -e . && rm -rf .git"
    ]
  },
  "inputs": [
    {
      "name": "channel1_path",
      "description": "Path to the first channel image file (fluorescent protein 1)",
      "type": "string",
      "required": true
    },
    {
      "name": "channel2_path",
      "description": "Path to the second channel image file (fluorescent protein 2)",
      "type": "string",
      "required": true
    },
    {
      "name": "output_dir",
      "description": "Directory to save output files",
      "type": "string",
      "required": false,
      "default": "./output"
    },
    {
      "name": "threshold_method",
      "description": "Method for thresholding images ('otsu', 'li', or 'yen')",
      "type": "string",
      "required": false,
      "default": "otsu"
    }
  ],
  "output": {
    "type": "string",
    "description": "Research log summarizing the colocalization analysis results and saved files"
  }
}
