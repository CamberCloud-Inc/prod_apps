{
  "name": "analyze_cell_morphology_and_cytoskeleton",
  "description": "Quantifies cell morphology and cytoskeletal organization from fluorescence microscopy images.",
  "icon": "🔬",
  "container": {
    "image": "python:3.9-slim",
    "setup_commands": [
      "git clone --depth 1 https://github.com/snap-stanford/Biomni.git /tmp/biomni && cd /tmp/biomni && pip install -e . && rm -rf .git"
    ]
  },
  "inputs": [
    {
      "name": "image_path",
      "description": "Path to fluorescence microscopy image file",
      "type": "file",
      "required": true
    },
    {
      "name": "output_dir",
      "description": "Directory for output results (default: ./results)",
      "type": "string",
      "required": false,
      "default": "./results"
    },
    {
      "name": "threshold_method",
      "description": "Thresholding method for segmentation (default: otsu)",
      "type": "string",
      "required": false,
      "default": "otsu"
    }
  ],
  "output": {
    "type": "directory",
    "path": "./results"
  }
}
