{
  "name": "query_scholar",
  "description": "Search Google Scholar for papers based on the provided query. Returns the first search result with title, year, venue, and abstract.",
  "version": "1.0.0",
  "author": "Biomni Literature Tools",
  "license": "MIT",
  "input": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search query string for Google Scholar"
      }
    },
    "required": ["query"]
  },
  "output": {
    "type": "object",
    "properties": {
      "success": {
        "type": "boolean",
        "description": "Whether the operation succeeded"
      },
      "result": {
        "type": "string",
        "description": "Formatted first search result with paper details"
      },
      "error": {
        "type": "string",
        "description": "Error message if operation failed"
      }
    }
  },
  "execution": {
    "command": "python3",
    "args": [
      "query_scholar.py",
      "{{query}}"
    ]
  }
}
