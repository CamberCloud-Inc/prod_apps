{
  "name": "advanced_web_search_claude",
  "description": "Conduct advanced web search using Claude AI to collect relevant information with citations through multiple rounds of web searches. Requires ANTHROPIC_API_KEY.",
  "version": "1.0.0",
  "author": "Biomni Literature Tools",
  "license": "MIT",
  "input": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "The search phrase for Claude to look up. Craft carefully for best results."
      },
      "max_searches": {
        "type": "integer",
        "description": "Upper-bound on searches Claude may issue inside this request",
        "default": 1
      },
      "max_retries": {
        "type": "integer",
        "description": "Maximum number of retry attempts with exponential backoff",
        "default": 3
      }
    },
    "required": ["query", "max_searches", "max_retries"]
  },
  "output": {
    "type": "object",
    "properties": {
      "success": {
        "type": "boolean",
        "description": "Whether the operation succeeded"
      },
      "result": {
        "type": "string",
        "description": "Formatted response from Claude with full text and citations"
      },
      "error": {
        "type": "string",
        "description": "Error message if operation failed"
      }
    }
  },
  "execution": {
    "command": "python3",
    "args": [
      "advanced_web_search_claude.py",
      "{{query}}",
      "{{max_searches}}",
      "{{max_retries}}"
    ]
  }
}
