{
  "name": "analyze_thrombus_histology",
  "description": "Analyze histological images of thrombus samples.",
  "icon": "🔬",
  "container": {
    "image": "python:3.9-slim",
    "setup_commands": [
      "git clone --depth 1 https://github.com/snap-stanford/Biomni.git /tmp/biomni && cd /tmp/biomni && pip install -e . && rm -rf .git"
    ]
  },
  "inputs": [
    {
      "name": "image_path",
      "description": "Path to H&E stained thrombus image",
      "type": "string",
      "required": true
    },
    {
      "name": "output_dir",
      "description": "Directory for saving output files (default: ./output)",
      "type": "string",
      "required": false,
      "default": "./output"
    }
  ],
  "output": {
    "type": "string",
    "description": "Research log summarizing analysis steps"
  }
}
