{
  "name": "analyze_atp_luminescence_assay",
  "description": "Analyze luminescence-based ATP concentration.",
  "icon": "💡",
  "container": {
    "image": "python:3.9-slim",
    "setup_commands": [
      "git clone --depth 1 https://github.com/snap-stanford/Biomni.git /tmp/biomni && cd /tmp/biomni && pip install -e . && rm -rf .git"
    ]
  },
  "inputs": [
    {
      "name": "data_file",
      "description": "CSV with luminescence readings",
      "type": "string",
      "required": true
    },
    {
      "name": "standard_curve_file",
      "description": "CSV with standard curve data",
      "type": "string",
      "required": true
    },
    {
      "name": "normalization_method",
      "description": "Method for normalizing ATP values (default: cell_count)",
      "type": "string",
      "required": false,
      "default": "cell_count"
    },
    {
      "name": "normalization_data",
      "description": "Cell count or protein content data",
      "type": "string",
      "required": false
    }
  ],
  "output": {
    "type": "string",
    "description": "Research log summarizing analysis steps"
  }
}
