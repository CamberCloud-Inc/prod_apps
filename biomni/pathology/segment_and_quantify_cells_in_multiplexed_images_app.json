{
  "name": "segment_and_quantify_cells_in_multiplexed_images",
  "description": "Segment cells and quantify protein expression levels from multichannel tissue images.",
  "icon": "🧫",
  "container": {
    "image": "python:3.9-slim",
    "setup_commands": [
      "git clone --depth 1 https://github.com/snap-stanford/Biomni.git /tmp/biomni && cd /tmp/biomni && pip install -e . && rm -rf .git"
    ]
  },
  "inputs": [
    {
      "name": "image_path",
      "description": "Path to multichannel image file",
      "type": "string",
      "required": true
    },
    {
      "name": "markers_list",
      "description": "Marker names for each image channel (as JSON array)",
      "type": "array",
      "required": true
    },
    {
      "name": "nuclear_channel_index",
      "description": "Index of nuclear marker channel (default: 0)",
      "type": "integer",
      "required": false,
      "default": 0
    },
    {
      "name": "output_dir",
      "description": "Directory for output files (default: ./output)",
      "type": "string",
      "required": false,
      "default": "./output"
    }
  ],
  "output": {
    "type": "string",
    "description": "Research log summarizing analysis steps and output file locations"
  }
}
