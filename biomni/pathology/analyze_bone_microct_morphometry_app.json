{
  "name": "analyze_bone_microct_morphometry",
  "description": "Analyze bone microarchitecture parameters from 3D micro-CT images including bone mineral density, bone volume, trabecular number/thickness/separation.",
  "icon": "🦴",
  "container": {
    "image": "python:3.9-slim",
    "setup_commands": [
      "git clone --depth 1 https://github.com/snap-stanford/Biomni.git /tmp/biomni && cd /tmp/biomni && pip install -e . && rm -rf .git"
    ]
  },
  "inputs": [
    {
      "name": "input_file_path",
      "description": "Path to micro-CT scan data file",
      "type": "string",
      "required": true
    },
    {
      "name": "output_dir",
      "description": "Directory for output files (default: ./results)",
      "type": "string",
      "required": false,
      "default": "./results"
    },
    {
      "name": "threshold_value",
      "description": "Threshold for bone segmentation (defaults to Otsu's method if None)",
      "type": "float",
      "required": false
    }
  ],
  "output": {
    "type": "string",
    "description": "Research log summarizing analysis steps and bone microarchitecture results"
  }
}
