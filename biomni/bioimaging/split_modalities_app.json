{
  "name": "Split Modalities",
  "description": "Split a 4D NIfTI file into separate modality files for nnUNet processing",
  "class": "bioimaging",
  "entrypoint": "split_modalities.py",
  "parameters": {
    "input_file": {
      "type": "file",
      "description": "Path to the 4D NIfTI file",
      "required": true
    },
    "output_dir": {
      "type": "string",
      "description": "Directory to save the split files",
      "required": true
    },
    "case_name": {
      "type": "string",
      "description": "Base name for the case (default: BRAT)",
      "required": false,
      "default": "BRAT"
    }
  },
  "returns": {
    "output_dir": {
      "type": "string",
      "description": "Path to directory containing split modality files"
    }
  },
  "setup_commands": [
    "git clone --depth 1 https://github.com/snap-stanford/Biomni.git /tmp/biomni 2>/dev/null || (cd /tmp/biomni && git pull)",
    "pip install -e /tmp/biomni",
    "pip install nibabel numpy SimpleITK torch matplotlib requests"
  ]
}
