{
  "name": "Calculate Similarity Metrics",
  "description": "Calculate similarity metrics between two medical images",
  "class": "bioimaging",
  "entrypoint": "calculate_similarity_metrics.py",
  "parameters": {
    "image1_path": {
      "type": "file",
      "description": "Path to first image",
      "required": true
    },
    "image2_path": {
      "type": "file",
      "description": "Path to second image",
      "required": true
    }
  },
  "returns": {
    "metrics": {
      "type": "object",
      "description": "Dictionary of similarity metrics (mutual_information, mean_squares, correlation, normalized_correlation)"
    }
  },
  "setup_commands": [
    "git clone --depth 1 https://github.com/snap-stanford/Biomni.git /tmp/biomni 2>/dev/null || (cd /tmp/biomni && git pull)",
    "pip install -e /tmp/biomni",
    "pip install nibabel numpy SimpleITK torch matplotlib requests"
  ]
}
