{
  "name": "analyze_protein_conservation",
  "description": "Perform multiple sequence alignment and phylogenetic analysis to identify conserved protein regions.",
  "icon": "🧬",
  "container": {
    "image": "python:3.9-slim",
    "setup_commands": [
      "git clone --depth 1 https://github.com/snap-stanford/Biomni.git /tmp/biomni && cd /tmp/biomni && pip install -e . && rm -rf .git"
    ]
  },
  "inputs": [
    {
      "name": "protein_sequences",
      "description": "List of protein sequences in FASTA format from multiple organisms",
      "type": "array",
      "required": true
    },
    {
      "name": "output_dir",
      "description": "Directory to save output files (default: ./)",
      "type": "string",
      "required": false,
      "default": "./"
    }
  ],
  "output": {
    "type": "string",
    "description": "Research log summarizing sequence alignment and conservation analysis"
  }
}
