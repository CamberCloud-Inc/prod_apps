{
  "name": "lammps-generic",
  "title": "Generic LAMMPS",
  "description": "Accepts path to LAMMPS project folder & .lmp filename as entry point for a simulation.",
  "content": "Accepts path to LAMMPS project folder & .lmp filename as entry point for a simulation.",
  "imageUrl": "",
  "command": "cd ${simulationFolder} && lmp -in ${scriptFilename}",
  "engineType": "LAMMPS",
  "jobConfig": [
    {
      "type": "Select",
      "label": "System Size",
      "name": "system_size",
      "hidden": true,
      "description": "Select the configuration for the job",
      "options": [
        {
          "label": "Small",
          "value": "small",
          "mapValue": {
            "nodeSize": "XXSMALL",
            "numNodes": 1,
            "withGpu": false
          }
        }
      ],
      "defaultValue": "small"
    }
  ],
  "spec": [
    {
      "type": "Input",
      "label": "Script Filename",
      "name": "scriptFilename",
      "description": "Specifies the LAMMPS input script to use for the simulation.",
      "defaultValue": "lj.lmp",
      "hidden": false,
      "required": true,
      "disabled": false
    },
    {
      "type": "Stash File",
      "label": "Simulation Folder",
      "name": "simulationFolder",
      "description": "Select the folder with LAMMPS project files among which the script is located.",
      "defaultValue": "",
      "hidden": false,
      "required": true,
      "disabled": false
    }
  ]
}